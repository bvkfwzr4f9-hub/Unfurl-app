export { colors } from './colors';
export { fontFamily, type } from './typography';
export { spacing, radius, shadow } from './spacing';

/**
 * Usage:
 *   import { colors, type, spacing } from '@/theme';
 *
 *   <View style={{ backgroundColor: colors.cream, padding: spacing.lg }}>
 *     <Text style={[type.h1, { color: colors.ink }]}>Unfurl</Text>
 *   </View>
 */
